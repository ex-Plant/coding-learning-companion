---
change_id: v1-sprint-plan
status: active
opened: 2026-05-26
target_completion: 2026-06-10
---

# v1 Sprint Plan

Implementation plan for shipping `Coding Learning Companion` v1 against the PRD's 2026-06-10 hard deadline. The plan breaks the PRD's 22 FRs into ordered phases with hour estimates against the realistic after-hours capacity of a solo developer.

See `plan.md` for the phase-by-phase breakdown and the cut order if running behind.

This change is not produced via `/10x-new` — it's a hand-rolled plan written immediately after the PRD's open questions were resolved (commit `0c6a1c7`). The same identity-file convention is followed so the folder reads consistently against other `context/changes/<id>/` folders.
